name: services-host-network
on: push
jobs:
  test1:
    runs-on: ubuntu-latest
    services:
      nginx:
        image: "nginx:latest"
        ports:
          - "8080:80"
    steps:
      - run: curl -v http://localhost:8080